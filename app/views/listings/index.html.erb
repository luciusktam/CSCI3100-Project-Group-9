<div class="app listings-page">
  <header class="navbar">
    <%= link_to root_path, class: "logo" do %>
      CUMarket
    <% end %>

    <div class="nav-links">
      <%= link_to listings_path do %>
        <i class="fas fa-store"></i> Market
      <% end %>
      <%= link_to community_path do %>
        <i class="fas fa-users"></i> Community
      <% end %>
      <%= link_to chat_path, id: "navChatLink", class: "nav-chat-link" do %>
        <i class="fas fa-comment-dots"></i> Chat
        <span id="chatNavBadge" class="nav-badge" style="display: none;">0</span>
      <% end %>
      <%= link_to sell_path do %>
        <i class="fas fa-euro-sign"></i> Sell
      <% end %>
      <% if user_signed_in? && current_user.admin? %>
        <%= link_to admin_dashboard_path do %>
          <i class="fas fa-shield-alt"></i> Admin
        <% end %>
      <% end %>
      <%= link_to profile_path do %>
        <i class="fas fa-user-circle"></i> Profile
      <% end %>
    </div>

    <div class="right-corner">
      <button id="theme-toggle" class="theme-toggle" type="button" aria-label="Toggle dark mode" aria-pressed="false">
        <i class="fas fa-moon"></i>
        <span>Dark</span>
      </button>

      <% if user_signed_in? %>
        <span class="session-badge">
          <i class="fas fa-user-check"></i>
          <%= current_user.username %>
          <% if current_user.admin? %>
            <strong>Admin</strong>
          <% end %>
        </span>

        <%= button_to logout_path, method: :delete, class: "login-btn logout-btn" do %>
          <i class="fas fa-sign-out-alt"></i> Logout
        <% end %>
      <% else %>
        <%= link_to login_path, class: "login-btn" do %>
          <i class="fas fa-sign-in-alt"></i> Login
        <% end %>
      <% end %>
    </div>
  </header>



  <%= render "listings/search_filters" %>

  <div class="section-header">
    <div class="section-actions">
      <% if user_signed_in? %>
        <%= link_to "Self Listings", listings_path(user_id: current_user.id), class: "community-btn secondary" %>
        <%= link_to "All Listings", listings_path, class: "community-btn secondary" %>
        <%= link_to "Create a Listing", sell_path, class: "create-listing-btn" %>
      <% else %>
        <%= link_to "Create a Listing", login_path, class: "create-listing-btn always-visible", data: { turbo: false } %>
      <% end %>
    </div>
  </div>

  <% if @listings.present? && @listings.any? %>
    <div class="listings-grid">
      <% @listings.each do |listing| %>
        <%= render partial: "listings/listing_card", locals: { listing: listing } %>
      <% end %>
    </div>

    <% if @listings.total_pages > 1 %>
      <div class="pagination">
        <%= paginate @listings %>
      </div>
    <% end %>
  <% else %>
    <div class="no-listings">
      <i class="fas fa-box-open"></i>
      <h3>No listings yet</h3>
      <p>Be the first to list an item!</p>
    </div>
  <% end %>
</div>
